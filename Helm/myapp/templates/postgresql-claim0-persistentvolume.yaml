apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgresql-claim0
  namespace: {{ .Values.namespace }}
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/Users/pashkov.vv/temp/postgres-data"
